import React from 'react';
import {Alert} from "@mui/material";

const BoardList = () => {
    return (
        <div>
            <Alert>게시판 목록</Alert>
        </div>
    );
};

export default BoardList;
